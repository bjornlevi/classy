<section>
	<% if signed_in? %>
		<%= link_to gravatar_for(current_user, size=40), current_user, 
		alt: "This should be a picture of you", title: "This should be a picture of you" %>
		</a>
	<% else %>
		&nbsp;
	<% end %>
</section>