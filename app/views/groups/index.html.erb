<table>
	<thead>
		<th>Group</th>
		<th>Creator</th>
		<th>Created</th>
	</thead>
	<% for group in @groups %>
		<tr>
			<td><%= group.name %></td>
			<td><%= User.find(group.user_id).email %></td>
			<td><%= time_ago_in_words(group.created_at) %></td>
		</tr>
	<% end %>
</table>